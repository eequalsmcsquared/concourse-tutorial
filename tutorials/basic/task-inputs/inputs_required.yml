# fly -t tutorial e -c inputs_required.yml -i pass-local-path-folder=.
# fly -t tutorial e -c inputs_required.yml -i pass-local-path-folder=../task-hello-world
---
platform: linux

image_resource:
  type: registry-image
  source: {repository: busybox}

inputs:
  - name: pass-local-path-folder

run:
  path: ls
  args: ["-alR"]
